# Log Anomaly CLI

A minimal developer tool that analyzes log files to surface rare log patterns.

## What it does
- Parses logs
- Fingerprints messages
- Identifies low-frequency patterns

This is a baseline version intended to be extended with spike detection,
scoring, and additional analysis.
